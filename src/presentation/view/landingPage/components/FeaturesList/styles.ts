'use client'
import styled from "styled-components";

export const ContainerListFeatures = styled.div`
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    margin-top: 1rem;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    margin-bottom: 4rem;
`